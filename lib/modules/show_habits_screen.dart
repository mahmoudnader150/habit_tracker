import 'package:flutter/material.dart';

class ShowHabitsScreen extends StatelessWidget {
  const ShowHabitsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Text("Show Habits"),
    );
  }
}
